# ====================================================
# Docker Environment Configuration
# ====================================================
# Настройки для работы в Docker-контейнерах
# Архитектура: PHP-FPM + Nginx (TCP) + PostgreSQL + pgAdmin

# --- Database Connection ---
# Важно! В основной секции .env замените DB_HOST=127.0.0.1
# на имя сервиса БД из вашего docker-compose.yml

# --- pgAdmin Web Interface ---
# Доступ к pgAdmin: http://localhost:8080
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin

# --- Xdebug Configuration ---
# По умолчанию отключен для производительности
# Для включения установите: XDEBUG_MODE=debug и XDEBUG_START=yes
XDEBUG_MODE=off
XDEBUG_START=no
XDEBUG_CLIENT_HOST=host.docker.internal

# ====================================================
# Пример конфигурации базы данных для Docker
# ====================================================
# Эти значения должны быть в основной части .env файла Laravel.
# Убедитесь, что они соответствуют настройкам из docker-compose.yml:

# DB_CONNECTION=pgsql
# DB_HOST=laravel-postgres-nginx-tcp  # ← Замените 127.0.0.1 на имя сервиса из docker-compose.yml
# DB_PORT=5432
# DB_DATABASE=laravel
# DB_USERNAME=postgres
# DB_PASSWORD=root
